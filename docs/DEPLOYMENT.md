# Deployment

Informações sobre a implantação e execução do backend.

## Como Rodar Localmente
INFORMAÇÃO NÃO CONFIRMADA. (Presume-se o uso padrão do Flask: configurar ambiente virtual, instalar dependências e rodar `flask run`).

## Como Fazer Deploy
INFORMAÇÃO NÃO CONFIRMADA.

## Onde Está Hospedado
O backend está hospedado na plataforma Railway. A URL base de produção é `https://web-production-684c4.up.railway.app`.

## Comandos Conhecidos
INFORMAÇÃO NÃO CONFIRMADA.

## Variáveis de Ambiente Necessárias
O sistema utiliza um arquivo `.env` carregado via `dotenv`. Variáveis inferidas incluem credenciais do banco de dados PostgreSQL e possivelmente chaves de integração do Mercado Pago.
